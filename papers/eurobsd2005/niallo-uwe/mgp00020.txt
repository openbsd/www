
Zaurus-specific porting considerations


Reference ports:

"cats" is another supported platform (StrongARM); use it as the starting point for the "zaurus" port

NetBSD has support for some of the features of the XScale PXA25x CPU which we can integrate

Development tools:

cross and native tools are readily available because cats binaries run on XScale CPUs


