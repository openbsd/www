Sugar and details

pkg_add has a progress bar.
pkg_add has quirks: quirks is a specific package that contains all exceptions to naming problems. 

So we handle:
renames
stuff in the base system

okay, database... if you can call a database a list of ~30 package names.

