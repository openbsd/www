Fun shitz

The "runner" in dpb is a specific class

        $core->shell->env(PKG_PATH => $path)->sudo
            ->exec(@cmd, (sort keys %$dep));

can be... whatever

more detached mode

chroot'd dpb + rsync
quick distributed replication
just a few rules
rebuild packages while desktop still runs
