Squiggles - how it works

On a machine  with lots of core,
devote one cpu to walking the queue in reverse

small ports get built as a trickle
no more IO bound at the end
no more waiting, fighting over dependencies
