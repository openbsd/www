GDB Support

gdb 6.3 doesn't support pie currently

Port of pie support from Linux

gdb needs to find out load offset too

New ptrace() PT_IO operation PIOD_READ_AUXV in -current

Breakpoints need to be deferred until program startup

Will be committed soon

Need to switch to ELF format for core files
