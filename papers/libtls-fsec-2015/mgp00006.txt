
libtls

libtls is a new API to replace the OpenSSL compatible API's that are in wide use today.

The current OpenSSL API in use is difficult to use and error prone.

"OpenSSL is why we can't have nice things."



